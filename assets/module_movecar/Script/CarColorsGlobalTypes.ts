export enum CarTypes {
    Sedan,
    Bus,
    Minivan,
}

export enum CarDir {
    TOP,
    BOTTOM,
    LEFT,
    RIGHT,
}

export enum CarColors {
    Blue,
    Green,
    Red,
    White,
    Yellow,
    Pink,
    Purple,
    Brown
}